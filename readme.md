# Unofficial Api Social network Badoo [![License][packagist-license]][license-url]

[![Downloads][packagist-downloads]][packagist-url]
[![Telegram][Telegram-image]][Telegram-url]


- [Installation](#Installation)
- [Example](#Example)
- Method Api
    - [User Auth](#userAuth)
    - [Load Session](#Load-Session)
    - [Get User](#Get-User)
    - [Search User](#Search-User)
    - [Get City](#Get-City)
    - [Get Visitors](#Get-Visitors)
    - [Like User](#Like-User)
    - [Send Message](#Send-Message)
- [Use Proxy](#Use-Proxy)

## Installation
**Using Composer:**
```
composer require tioffs/badoo
```
## Example
```php
require_once __DIR__ . '/vendor/autoload.php';
$badoo = new UnofficialApi\Badoo('login', 'password');
```
# Method Api
## userAuth
Log in and get the session
```php
$auth = $badoo->userAuth();
#success
['user' => 0123456789]
#error
['error' => 'Authorization false']
```
## Load Session
Load authorized session
```php
$sessionLoad = $badoo->loadSession();
#success
true
#error
false
```
## Get User
Get user profile information
```php
#default return current user profile
$dataUser = $badoo->getUser();
#return user profile
$dataUser = $badoo->getUser(123456789);
#data user array
[
    'age' => 23,  //user age
    'albums' => [], //albums photo array
    'displayed_about_me' => [], //profile about text array
    'dob' => '1996-02-02' // date of birth
    'gender': 1, // 1 - men, 2 - women
    'is_blocked': false,
    'is_deleted': false,
    'is_favourite': false,
    'name' => 'Nikolas', // user name
    'online_status' => 3, // 1 - online, 2,3 - was online time
    'online_status_text' => 'Was online 8 hours ago',
    'photo_count' => 2, //count photo
    'profile_fields' => [], //the information is filled
    'profile_photo' => [], //current user photo
    'user_id' => 123345644 // user id

]
```
## Search User
Search for users with a filter
```php
$gender = 1; // 1 - men, 2 - women
$AgeStart = 18; // filter start age
$AgeEnd = 60; // filter end age
$count = 150; // count users
$offset = 1; //offset count
$country = ''; //default all city ''; use method getCity
$user = $badoo->searchUser($gender, $AgeStart, $AgeEnd, $count, $offset, $country);
#result data array users
```
## Get City
Obtaining the data for the subsequent search filter
```php
$city = $badoo->getCity('Moscow');
#success
countryId_regionId_cityId
#error
null
```
## Get Visitors
This method returns the list of visitors for the last week
```php
$data = $badoo->getVisitors();
#result data array users
```
## Like User
Liking user profile
```php
$userId = 123345644;
$badoo->likeUser($userId);
#success
true
#error
false
```
## Send Message
Send private message to user
```php
$userId = 123345644;
$message = 'test message';
$badoo->sendMessage($userId, $message);
#success
uid message
#error
false
```
## Use Proxy
```php
#use proxy http/https
$badoo->setProxy('127.0.0.1', '8080')
#use proxy authorization
$badoo->setProxy('127.0.0.1', '8080', 'login', 'password')
```

----

Made with &#9829; from the [@tioffs][tioffs-url]

[tioffs-url]: https://timlab.ru/
[license-url]: https://github.com/tioffs/badoo/blob/master/LICENSE

[telegram-url]: https://t.me/joinchat/C9JmzQ-fc3SKXI0D-9h-uw
[telegram-image]: https://img.shields.io/badge/Telegram-Join%20Chat-blue.svg?style=flat

[packagist-url]: https://packagist.org/packages/tioffs/badoo
[packagist-license]: https://img.shields.io/github/license/tioffs/badoo
[packagist-downloads]: https://img.shields.io/packagist/dm/tioffs/badoo

15.02.26
Ось результати аналізу та стратегія трансформації для проекту **badoo** (Unofficial API), підготовлені у форматі для Notion.

---

# 📑 Звіт AI-консультанта: Проект "badoo"

## 🧬 Частина 1: "ДНК" Проекту

Проект **badoo** — це неофіційний API для соціальної мережі Badoo, написаний на мові **PHP** (100%). Його логіку можна розбити на такі **атомарні функції**:

*   **Авторизація та керування сесіями (Auth Management):** Функції `userAuth` для входу за логіном/паролем та `loadSession` для відновлення вже існуючої сесії.
*   **Вилучення даних (Data Parsing):** Функція `getUser` дозволяє отримувати детальні профілі (вік, фото, статус "online", текст "про себе").
*   **Пошук та фільтрація (Discovery):** Модуль `searchUser` з можливістю фільтрації за статтю, віком та геопозицією, а також функція `getCity` для уточнення локації.
*   **Взаємодія з користувачами (Engagement):** Методи `likeUser` для проставляння вподобань та `sendMessage` для надсилання приватних повідомлень.
*   **Аналітика активності:** Метод `getVisitors` для отримання списку відвідувачів профілю за останній тиждень.
*   **Мережева анонімність:** Підтримка роботи через проксі-сервери (HTTP/HTTPS) з авторизацією через `setProxy`.

### 💎 Головна технічна цінність
Головна цінність проекту полягає в **автоматизації складних користувацьких сценаріїв** у закритій екосистемі соціальної мережі. Він надає програмісту "ключі" від функцій Badoo без необхідності використовувати веб-інтерфейс, що дозволяє створювати інструменти для масового маркетингу, аналітики або автоматичного дейтингу.

---

## 🚀 Частина 2: "Трансформація" (Інтеграція з Gemini LLM)

*Примітка: Інформація про інтеграцію з Gemini та сценарії використання LLM є моїм аналітичним прогнозом, оскільки в джерелах згадуються лише інструменти GitHub для розробки AI (Copilot, Spark), але не конкретна реалізація в цьому коді.*

Додавання Gemini перетворює цей API на **автономного інтелектуального асистента (AI Agent)**.

### Як зміниться функціонал?
1.  **AI-персоналізація повідомлень:** Замість шаблонних текстів у `sendMessage`, Gemini аналізуватиме масив `displayed_about_me` та генеруватиме унікальний "криголам" (icebreaker) для кожного користувача.
2.  **Семантичний пошук:** Замість жорстких фільтрів за віком, ви зможете ставити запити: *"Знайди людей, які цікавляться класичною музикою та мають активний спосіб життя"*. Gemini шукатиме відповідності в текстових описах профілів.
3.  **Автоматичний скоринг:** AI оцінюватиме якість фото та змістовність профілю, виділяючи найбільш релевантних кандидатів для взаємодії.

### Сценарій сервісу (Badoo API + ID_{$} + Gemini)

Уявімо створення сервісу **"Smart Dating Manager"** на вашому сайті.

**Алгоритм роботи:**
1.  **Вхід (ID_{$}):** Ваш Python-скрипт отримує налаштування від клієнта через ваш сайт (наприклад: "Шукаю співрозмовника для вивчення англійської").
2.  **Збір (PHP API):** Python-скрипт через системний виклик або мікросервіс запускає функцію `searchUser` для збору профілів у вашому місті.
3.  **Аналіз (Gemini):** Python-скрипт передає отримані через `getUser` дані профілів до Gemini. AI фільтрує тих, хто вказав у інтересах мови або освіту.
4.  **Комунікація (PHP API):** Для відібраних профілів Gemini генерує ввічливе привітання, а функція `sendMessage` автоматично його відправляє.
5.  **Звіт:** Ви отримуєте на сайті повідомлення лише тоді, коли AI отримав відповідь, що заслуговує на вашу увагу.

---

## 📋 План дій для Notion
| Крок | Дія | Результат |
| :--- | :--- | :--- |
| **1** | Встановлення пакету через `composer require tioffs/badoo` | Підготовка PHP-середовища |
| **2** | Створення Python-обгортки (ID_{$}) для виклику PHP-методів | Можливість керування з вашого бекенду |
| **3** | Підключення Gemini API для аналізу поля `displayed_about_me` | Додавання інтелекту до профілів |
| **4** | Налаштування GitHub Actions для автоматизації роботи бота | Стабільна робота сервісу 24/7 |

---

### 💡 Резюме

**Суть:** Неофіційний API для автоматизації Badoo.

**AI-Роль:** Створення персоналізованих комунікацій та розумного пошуку.

*(Оцінка сценаріїв з AI базується на технічних можливостях обробки даних профілю, описаних у джерелах).*
